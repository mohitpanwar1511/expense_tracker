name: expense_tracker
description: A premium, minimal offline expense tracker application.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  sembast: ^3.7.4
  path_provider: ^2.1.1
  path: ^1.9.0
  fl_chart: ^0.66.0
  google_fonts: ^6.1.0
